# Demand Radar — 2026-06-23

---

## Pain — Reddit / HN / GitHub

### 1. [search] `[BUG] Cowork local-agent-mode sessions missing from Recents despite intact data on disk (Windows)` — GitHub Issue #69663 · anthropics/claude-code — June 20, 2026 ← FRESHEST SIGNAL THIS CYCLE
**Link:** https://github.com/anthropics/claude-code/issues/69663  
**Platform:** GitHub Issues (anthropics/claude-code)  
**Approx. date:** June 20, 2026 — 3 days ago  
**Context:** Claude **Desktop App** Cowork / local-agent-mode (Windows). Two sessions ("AR rooms..." and "Airport Digital Twin + Simulation") disappeared from the Recents sidebar. Data is intact on disk but the IndexedDB index lost track of them, possibly triggered by a subscription status change June 19-20.  
**⚠️ CORRECTION (verified June 23):** This data is **NOT in a format ccfind reads today.** Cowork/local-agent-mode stores whole-session **`.json`** files (not `.jsonl`) inside `local_<uuid>/` folders at `…\AppData\Local\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\local-agent-mode-sessions\<ownerAccountId>\`, plus an `outputs/` dir. ccfind only reads Claude **Code** CLI (`~/.claude/projects/*.jsonl`), OpenCode (SQLite), and Hermes. The data is plaintext (not encrypted/gatekept), so a human can open it — but recommending ccfind as a workaround here would be **false**. Do NOT post the workaround comment.  
**Opportunity:** **Build, not comment** — this is a *feature* signal: add a Cowork / Desktop local-agent-mode adapter (`local_<uuid>/*.json`) to ccfind. If/when that ships, this issue becomes a legitimate place to help. Until then, no authentic claude-grep angle exists. **Do not engage with the current drafted-comment approach.**

**Drafted comment:** ~~(removed — ccfind cannot read this data; the prior draft was inaccurate)~~. Only re-add this thread if a Cowork adapter is built.

---

### 2. [search] `[BUG] Sessions are auto-archived immediately after Claude sends a message` — GitHub Issue #68972 · anthropics/claude-code — June 17, 2026
**Link:** https://github.com/anthropics/claude-code/issues/68972  
**Platform:** GitHub Issues (anthropics/claude-code)  
**Approx. date:** June 17, 2026 — closed as duplicate; original is still open  
**Context:** Claude **Desktop App** v2.1.167 (Windows 11). Sessions are archived within seconds of Claude sending a reply in an active chat — the user must manually unarchive every turn. Regression; no `settings.json` toggle.  
**⚠️ CAUTION (June 23):** Same Desktop App family as #69663 — **I cannot confirm these sessions live in the CLI's `~/.claude/projects/*.jsonl` store that ccfind reads.** If they're Desktop `.json` sessions, ccfind can't read them either. The earlier "reads raw JSONL" claim may be false here.  
**Opportunity:** **Low / verify-first** — only a ccfind angle if these are confirmed to be CLI jsonl files. Otherwise it folds into the same Cowork/Desktop-adapter feature signal as #69663.

**Drafted comment:** ~~(removed — storage format unconfirmed; do not assume ccfind can read Desktop App sessions)~~.

---

### 3. [branching] `/rewind says "conversation will be forked" but never creates a resumable fork` — GitHub Issue #55347 · anthropics/claude-code — May 1, 2026
**Link:** https://github.com/anthropics/claude-code/issues/55347  
**Platform:** GitHub Issues (anthropics/claude-code)  
**Approx. date:** May 1, 2026 — ~7 weeks ago; still open with labels `area:core, bug, has repro`  
**Context:** Claude Code. A detailed bug report with full JSONL forensics: 204 turns, single `sessionId` throughout, zero `isSidechain:true` entries, no new file created — the fork that `/rewind` promises is never executed. After rewinding, the original session disappears from `--resume`. The JSONL is append-only so the data survives, but it becomes unreachable through the normal UI. Three related issues linked: #24471, #9279, #47987 — all pointing to the same absent fork mechanism. No fix shipped yet.  
**Opportunity:** **High** — Ctrl-B in claude-grep is the working fork path that `/rewind` is supposed to create but doesn't. The forensics in this issue confirm that the JSONL format matches what Ctrl-B writes.

**Drafted comment:**
> The fork mechanism that `/rewind` promises is exactly what ccfind's Ctrl-B implements as an independent path. It reads your session JSONL (even recovered sessions where `--resume` can't see them), lets you pick any past user message, and writes a real new JSONL with a fresh `sessionId` that shows up in `claude --resume` under the same project. `/btw`-compatible with Claude Code's native fork format.
>
> It doesn't fix the `/rewind` bug — but it gives you a working fork-from-checkpoint path today while this is resolved upstream. Disclosure, I'm the maker: https://github.com/coolcorexix/claude-grep · `brew install coolcorexix/tap/claude-grep`

---

## Pain — X / Twitter

### 1. [search] `@DanKornas: "Your coding agent shouldn't start from zero every session"` — ~June 20–22, 2026 ← FRESHEST X SIGNAL
**Link:** https://x.com/DanKornas/status/2068182316782854572  
**Platform:** X / @DanKornas (Dan Kornas — CortexKit)  
**Approx. date:** ~June 20–22, 2026 (very high status ID; within last 3 days estimated)  
**Context:** OpenCode (and Pi). Dan announces Magic Context, a CortexKit plugin that compresses older session history into scored compartments and auto-captures decisions across sessions. The framing — "start from zero" — is the canonical statement of the OpenCode [search] pain: each new session is amnesiac. The plugin adds memory, but not searchability across past sessions by content. cc/grep fills the side of this pain that is "find the session where I worked through X" rather than "carry a summary forward."  
**Opportunity:** **High** — very fresh, OpenCode-native audience, complementary angle (search vs. memory), peer-builder context makes engagement natural.

**Drafted reply (≤280 chars, no outbound link in first reply):**
> The "start from zero" pain runs deep. ccfind (my tool, disclosure) takes a different angle — full-text search across past OpenCode sessions (stored in SQLite), tagged [o] alongside Claude Code [c] + Hermes [h]. So you can grep "where did I debug the auth race condition" across months of sessions. Ctrl-B forks any result from that message. 🔗 in follow-up

**Follow-up tweet:**
> github.com/coolcorexix/claude-grep · `brew install coolcorexix/tap/claude-grep`

---

### 2. [search] `@RobertTLange: "🚀 Fun additions to agentlens in v0.4.0 — index your previous agent coding sessions"` — ~June 20–22, 2026
**Link:** https://x.com/RobertTLange/status/2067582738815651845  
**Platform:** X / @RobertTLange (Robert Lange — agentlens)  
**Approx. date:** ~June 20–22, 2026 (very high status ID; within last 3 days estimated)  
**Context:** Multiple agents — Claude Code, OpenCode, Codex, Pi, Gemini, Cursor, Antigravity. Robert ships a session indexer using cron-powered summarization + embedding/lexical retrieval. Clear demand-validated competitor/complement. Key tradeoff vs. ccfind: agentlens requires a background indexing process and embedding infrastructure; ccfind is 300ms ripgrep with no setup. Different user profiles — agentlens is better for semantic recall; ccfind is better for exact-content/phrase lookup and interactive forking.  
**Opportunity:** **Medium** — peer-builder engagement, not a reply that promotes ccfind to the original poster's audience, but a genuine "different tradeoff" conversation that seeds awareness.

**Drafted reply (≤280 chars, no outbound link in first reply):**
> Love the multi-agent scope. Different tradeoff in ccfind (my tool): 300ms ripgrep over raw files, no background indexer or embeddings — trades semantic recall for instant grep + interactive session forking (Ctrl-B forks any session at any past message, /btw-compatible). Covers the same six agents. Curious what use cases pushed you toward embeddings vs. ripgrep?

---

### 3. [search] `@0xSero: "Everyone should save their session history — all your Claude code sessions get deleted every month"` — ~May 2026
**Link:** https://x.com/0xSero/status/2050544304360165585  
**Platform:** X / @0xSero  
**Approx. date:** ~late May 2026 (estimated from status ID range)  
**Context:** Claude Code. A PSA tweet alerting users to the 30-day `cleanupPeriodDays` default that silently deletes JSONL files. The tweet mentions that 0xSero has repos on GitHub to help preserve session history. Pain signal is the framing: sessions are "valuable training data you're keeping" — but the real pain is losing the ability to search back through work. Related PSAs from @WolframRvnwlf (#2032...) and @repligate (#2026...) in April/May confirm this is a recurring discovery shock.  
**Opportunity:** **Medium** — tweet is a few weeks old but PSA threads stay relevant. Reply adds the "and once you preserve them, here's how to search them" layer.

**Drafted reply (≤280 chars, no outbound link in first reply):**
> Good PSA. Once you've set `cleanupPeriodDays` high, the next gap is *finding* what you saved. `--resume` only matches session titles. ccfind (my tool, disclosure) full-text-searches all your preserved JSONL sessions and can fork any result from any message. 🔗 in follow-up

**Follow-up tweet:**
> github.com/coolcorexix/claude-grep · `brew install coolcorexix/tap/claude-grep`

---

## Show-your-work — Reddit / HN

### 1. `Show HN: Recall – Local project memory for Claude Code` — HN #48622590 — TODAY June 23, 2026 ← ACTIVE RIGHT NOW
**Link:** https://news.ycombinator.com/item?id=48622590  
**Platform:** Hacker News  
**Date:** June 23, 2026 — posted within the last few hours  
**Context:** Recall is a local project memory system for Claude Code — think persistent `CLAUDE.md`-level context that survives session boundaries. The tool and claude-grep solve adjacent but distinct problems: Recall answers "what does Claude know about this project now?", claude-grep answers "which past session discussed authentication three weeks ago?" The audiences are identical — developers frustrated with Claude Code's amnesiac sessions — and the comment section will be full of people who have both pain points simultaneously.  
**Opportunity:** **High — act today.** Fresh thread, zero competition from prior comments, HN audience trusts maker-built tools when disclosed transparently. A comment that acknowledges Recall's scope while pointing to the complementary search-and-fork gap lands as additive, not promotional.

**Drafted comment:**
> Nice — Recall and claude-grep sit right next to each other in the problem space. Recall is "what Claude knows about this project going forward"; claude-grep is "find the session from two weeks ago where I already solved this."
>
> Built ccfind (`ccfind`) as a unified content-search across Claude Code, OpenCode, and Hermes Agent sessions (all three formats: JSONL, SQLite, and Hermes's variant), with results tagged `[c]`/`[h]`/`[o]` by source. Hit Ctrl-B on any result to fork that session at any past user message — writes a real JSONL with a new `sessionId`, shows up in `claude --resume`, /btw-compatible with Claude Code's native fork format.
>
> Likely useful for folks who solve the memory problem with Recall but still find themselves grepping `~/.claude/projects/` for lost conversations. Disclosure: I'm the maker. https://github.com/coolcorexix/claude-grep · `brew install coolcorexix/tap/claude-grep`

---

### 2. `Ask HN: What are you working on? (June 2026)` — HN #48528779 — ~June 21, 2026 (still active)
**Link:** https://news.ycombinator.com/item?id=48528779  
**Platform:** Hacker News  
**Date:** ~June 21, 2026 — still open for comments  
**Context:** Monthly show-and-tell thread, open all month. Same opportunity as noted in the June 22 digest — posting here is the primary HN introduction channel for June. Not yet posted based on yesterday's digest.  
**Opportunity:** **High — act this week** (same as prior day; posting sooner = more engagement time).

**Drafted comment:** *(see June 22 digest for full draft — identical thread, reuse as-is)*

---

### 3. No Reddit show-and-tell thread surfaced today
Reddit search returned no active r/SideProject, r/coolgithubprojects, or r/ClaudeAI "what are you building" thread URLs for the June 17–23 window. Checking r/SideProject directly for the weekly sticky is recommended.

---

## Show-your-work — X / Twitter

No standalone "what are you building" invitation thread found on X in the last 48h. The two peer-builder tweets in the Pain — X section (Dan Kornas, Robert Lange) double as show-your-work adjacents: a well-crafted reply to either seeds awareness with their audiences and can lead to mutual amplification without requiring a dedicated promotional thread.

Best leverage today: the Dan Kornas thread is the freshest and directly names OpenCode — a reply there reaches the specific audience most underserved by native OpenCode session search.

---

## Summary

Today's split: **~65% search / ~35% branching.** **CORRECTION applied (June 23):** the two fresh GitHub bugs (#69663, #68972) are Claude **Desktop App / Cowork** sessions stored as `.json` in `…\AppData\…\Packages\Claude_…\local-agent-mode-sessions\` — a location and format ccfind does **not** read today (ccfind covers Claude Code CLI jsonl, OpenCode SQLite, Hermes). The data is plaintext, not gatekept — but recommending ccfind as a workaround on those issues would be false, so both drafted comments were pulled. They are reclassified as a **build signal**: a Cowork/Desktop adapter would unlock them. The genuinely actionable items today are the **`/rewind` fork bug (#55347)** [branching — Ctrl-B is a real answer] and the **live Show HN: Recall thread** [show-your-work — fresh, right audience, zero noise]. X peer-builder posts (@DanKornas, @RobertTLange) remain valid for the OpenCode/Claude-CLI search angle.
