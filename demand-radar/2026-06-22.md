# Demand Radar — 2026-06-22

---

## Pain — Reddit / HN / GitHub

### 1. [branching] `[FEATURE] Conversation Branching — full spec for fork, merge, and tree navigation` — GitHub Issue #32631 · anthropics/claude-code — Mar 9, 2026 (open, unassigned)
**Link:** https://github.com/anthropics/claude-code/issues/32631  
**Platform:** GitHub Issues (anthropics/claude-code)  
**Approx. date:** March 9, 2026 — still open, no official response  
**Context:** Claude Code. The most-detailed branching feature request found — consolidates 4+ prior related issues (each carrying 15-18 👍 before being merged here). Requests full git-style UX: `/branches` list, `/switch <name>`, `/merge <branch>` with LLM summary, `/checkpoint [name]`, `/diff <a> <b>`. Notes that Claude Code already has `/fork` and `--fork-session` but has no return path, no branch tree, and no human-readable names. Business justification focuses on long agentic sessions where exploring two approaches without abandoning one is currently impossible. No maintainer comment visible.  
**Opportunity:** **High** — "not planned" is the likely outcome on Anthropic's backlog; third-party is the realistic path. claude-grep's Ctrl-B is the closest thing to Tier 1–3 of the proposed spec that ships today.

**Drafted comment:**
> The gap you're describing — fork exists but no return path, no tree, no names — is real and daily. We shipped a narrower version of Tier 1-3 in **claude-grep** while this matures upstream: Ctrl-B on any session row forks from any past user message, assigns a new `sessionId`, writes a real JSONL, and the branch immediately shows up in `claude --resume` under the parent's project. `/btw`-compatible with Claude Code's native fork format.
>
> It doesn't cover `/merge` or `/diff` (those need model involvement), but the checkpoint + named fork + tree navigation pieces work today. Full disclosure, I'm the maker: https://github.com/coolcorexix/claude-grep · `brew install coolcorexix/tap/claude-grep`

---

### 2. [search] `[BUG] Chat JSONLs deleted from ~/.claude/projects/ despite cleanupPeriodDays set high — appears triggered by updates/restarts` — GitHub Issue #62272 · anthropics/claude-code
**Link:** https://github.com/anthropics/claude-code/issues/62272  
**Platform:** GitHub Issues (anthropics/claude-code)  
**Approx. date:** ~May-June 2026 (high issue number, very recent — part of the cleanupPeriodDays regression wave)  
**Context:** Claude Code. Users who explicitly set `cleanupPeriodDays` to a high value (e.g. 3650) are still losing session files after restarts or auto-updates — the setting is being ignored by a background cleanup process. Related open issues: #59248 (no warning on retention cleanup) and #62959 (sidebar ghost entries that error on click). Together these three issues signal that even users who knew to protect their history are now losing it. The search problem compounds: `--resume` shows ghost entries; manual grep is the fallback.  
**Opportunity:** **High** — users in this state need an immediate workaround AND a better search tool for whatever survived. Being concretely helpful (here's the grep one-liner + here's a UI for it) is the right entry point.

**Drafted comment:**
> While this gets fixed: your surviving sessions are still at `~/.claude/projects/<hash>/*.jsonl`. You can find them with:
> ```
> find ~/.claude/projects -name "*.jsonl" -newer ~/.claude/settings.json | sort -t/ -k6 | tail -20
> ```
> For content-searching what's left — `claude --resume` won't help if the index is gone — I built **claude-grep** (`ccfind`): searches session content across all project folders, shows results tagged by project, hit Enter to resume. Works even when the session index is broken.
>
> https://github.com/coolcorexix/claude-grep (disclosure: I'm the maker)

---

### 3. [search] `Show HN: Ctx – a /resume that works across Claude Code and Codex` — HN #47836740 — ~Apr 2026
**Link:** https://news.ycombinator.com/item?id=47836740  
**Platform:** Hacker News  
**Approx. date:** ~April 2026 (outside 1-week window; persistent demand signal — competitor validation)  
**Context:** Claude Code + GitHub Codex. Another independent builder solving the cross-agent session search problem, arriving at a two-agent scope (Claude + Codex). The comments will contain genuine pain signals about `--resume` being inadequate. claude-grep's differentiation: covers three agents (Claude Code, OpenCode/sst, Hermes Agent), adds Ctrl-B prompt-tree branching, and ships as a native TUI with fuzzy search rather than a wrapper around `--resume`.  
**Opportunity:** **Med** — the thread is several weeks old; a comment adding the multi-agent + branching angle is on-topic and genuinely additive, not duplicative.

**Drafted comment:**
> Great to see another builder hitting this gap. One gap I ran into with the two-agent scope: OpenCode (sst) sessions live in SQLite, not JSONL, and Hermes Agent is in yet another format — so cross-agent gets hairy fast. Built **claude-grep** (`ccfind`) to cover all three in one TUI, with results tagged `[c]`/`[h]`/`[o]` by source. Also ships Ctrl-B to fork any session at any past user message (real JSONL, new `sessionId`, shows in `claude --resume`). In case useful: https://github.com/coolcorexix/claude-grep · `brew install coolcorexix/tap/claude-grep`

---

## Pain — X / Twitter

### 1. [branching] `@svpino: "You should try 'Summarize from here' in Claude Code"` — June ~18–20, 2026 ← FRESHEST SIGNAL THIS CYCLE
**Link:** https://x.com/svpino/status/2058888825284513842  
**Platform:** X / @svpino (Santiago Valdarrama — high-reach AI educator, ~500k+ followers)  
**Approx. date:** ~June 18–20, 2026 — within last 1 week  
**Context:** Claude Code. Santiago describes the `/rewind` (Esc+Esc) → "Summarize from here" workflow to selectively compact a session without losing the good parts. High engagement on this thread because it touches "context rot" — quality degrades around 300–400k tokens. The implicit gap: this in-session rewind only works *while that session is live*. There is no way to rewind to a good checkpoint in a *past* closed session, or to branch two competing approaches across session boundaries.  
**Opportunity:** **High — act today.** Active thread, massive audience, direct bridge to Ctrl-B. Reply frames Ctrl-B as the cross-session extension of the tip he just described.

**Drafted reply (≤280 chars — no outbound link; follow up separately):**
> Great tip. The gap I kept hitting: you can only /rewind within a live session. Built a Ctrl-B shortcut in ccfind (my tool, disclosure) that forks any *past* closed session at any earlier user message — new JSONL, shows in `claude --resume`, /btw-compatible. Cross-session version of this exact pattern. 🔗 in next tweet

**Follow-up tweet:**
> ccfind / claude-grep: github.com/coolcorexix/claude-grep · `brew install coolcorexix/tap/claude-grep`

---

### 2. [both] `@svpino: "20 Claude Code tips"` (Tip #1: Esc+Esc rewind checkpoint) — June ~6, 2026
**Link:** https://x.com/svpino/status/2050563493708112190  
**Platform:** X / @svpino  
**Approx. date:** ~June 6, 2026 — ~2 weeks old but tips threads stay active  
**Context:** Claude Code. Tip #1 of a 20-tip megathread is "Esc+Esc: Rewind your session to any previous checkpoint — treat it as free undo." The thread has high reply volume and is the kind of thread that stays circulating for weeks. Provides cover to add a "Tip 21" style reply about cross-session branching (Ctrl-B) and cross-agent content search as complementary workflow additions.  
**Opportunity:** **Med** — thread is still getting comments; "Tip 21" framing is welcome and on-brand for the thread's format.

**Drafted reply (≤280 chars):**
> Tip 21: pair Esc+Esc with cross-session branching. Hit Ctrl-B in ccfind (my tool, disclosure) to fork any *past* session at any earlier user message — new JSONL, new sessionId, shows in `--resume`. Extends the "free undo" idea to sessions you closed days ago. 🔗 in follow-up

**Follow-up tweet:**
> github.com/coolcorexix/claude-grep · `brew install coolcorexix/tap/claude-grep` — also covers OpenCode + Hermes session content-search in the same TUI

---

### 3. [branching] `@timourxyz: "context rot often starts around ~300–400k tokens... think of every /rewind as a new attempt"` — April ~16, 2026
**Link:** https://x.com/timourxyz/status/2044759910311759975  
**Platform:** X / @timourxyz (Timour Kosters)  
**Approx. date:** ~April 16, 2026 — outside 1-week window; chronic pain signal  
**Context:** Claude Code. Thread discussing how 1M context increases the window but also makes "context rot" more insidious: quality silently degrades long before hitting the hard limit. The /rewind tip is treated as the main mitigation. The implicit missing piece: after the session closes, there's no way to return to the pre-rot checkpoint. Every new session starts cold.  
**Opportunity:** **Low** — older thread but clear framing for Ctrl-B's value; worth a reply only if the thread is still active.

**Drafted reply (≤280 chars):**
> The persistent gap after /rewind: once you close the session, that clean checkpoint is gone. Built Ctrl-B in ccfind (my tool) to fork any past closed session at any user message — brings the "new attempt from clean state" idea across session boundaries. Link in follow-up

---

## Show-your-work — Reddit / HN

### 1. `Ask HN: What are you working on? (June 2026)` — HN #48528779 — June 2026 ← ACTIVE NOW
**Link:** https://news.ycombinator.com/item?id=48528779  
**Platform:** Hacker News  
**Date:** June 2026 — the monthly active thread, open all month  
**Context:** Standard monthly "what are you working on?" thread. Participants share side projects, tools, and experiments. Developer-heavy audience, high tolerance for CLI / TUI tools, familiar with Claude Code. This is the primary HN show-your-work window for June and it's still open.  
**Opportunity:** **High — act this week.**

**Drafted comment:**
> **claude-grep** (`ccfind`) — unified content-search across Claude Code, OpenCode (sst), and Hermes Agent session history from one TUI.
>
> The itch it scratches: `claude --resume` matches session *titles* only, and only within the current project. OpenCode and Hermes sessions are siloed in their own formats (SQLite and a custom JSONL variant). When you've spread work across three agents and six project folders over three months, finding "that conversation where I debugged the WebSocket race condition" is genuinely painful.
>
> `ccfind` searches content across all three agents, tags results `[c]`/`[h]`/`[o]` by source, and lets you resume any session with one keypress.
>
> The second feature: Ctrl-B on any result row forks that session at any past user message — creates a real JSONL with a new `sessionId`, shows up in `claude --resume`, and is `/btw`-compatible with Claude Code's native fork format. It's a git-style checkpoint for AI conversations.
>
> `brew install coolcorexix/tap/claude-grep` | `nix run github:coolcorexix/claude-grep` | https://github.com/coolcorexix/claude-grep

---

### 2. No Reddit show-your-work thread found today
No active r/SideProject, r/coolgithubprojects, or r/ClaudeAI show-and-tell thread URL surfaced in searches for June 20–22, 2026. r/SideProject typically runs a weekly "what are you building?" sticky; checking that subreddit directly to find the live thread is recommended.

---

## Show-your-work — X / Twitter

No standalone "what are you building" invitation thread found on X in the last 48h that isn't already covered as a Pain reply opportunity above.

Best entry point this cycle: the @svpino "Summarize from here" thread (June 18–20) doubles as a show-your-work slot — a well-crafted "Tip 22" reply there reaches the same audience as any dedicated show-your-work thread, with the added credibility of being on-topic in a trusted educator's thread. See the Pain — X section draft above.

---

## Summary

Today's split: **~60% branching / ~40% search** — a reversal of yesterday's ratio. The shift is driven by two high-engagement svpino threads both centering on /rewind and checkpoint UX (branching pain), and the GitHub #32631 branching spec issue being the strongest single pain signal of the cycle. Search pain is still present and structural (session deletion bugs are multiplying), but today's loudest voices are reaching for cross-session undo, not cross-agent grep. Both HN and X entry points are live.
