# Demand Radar — 2026-06-17

---

## Pain — Reddit / HN / GitHub

### 1. [search] GitHub Issue #43696 — Session context not restored on --resume/--continue
**Link:** https://github.com/anthropics/claude-code/issues/43696  
**Platform:** GitHub · anthropics/claude-code  
**Date:** April 4, 2026 (closed as duplicate)  
**Agent(s):** Claude Code  
**Opportunity:** Low for direct engagement (closed); high as demand confirmation — the parent issue is almost certainly still open and actively followed.

**Context:** User filed a detailed report that `--resume` and `--continue` silently drop all conversation context, starting sessions fresh. Quote: *"This is a significant productivity loss for users doing extended work sessions. A multi-hour or multi-day session worth of context — file contents read, architectural decisions, in-progress work state — is irrecoverable."* The issue notes *no workaround exists*. Closed as a duplicate of earlier reports, confirming this is a chronic, recurring pain.

**Drafted reply (if a related open issue or PR discussion surfaces):**
> The content-search gap compounds this: even after you've kept your session files safe, `--resume` only matches by session title — you can't grep *what you discussed*. I built `ccfind` for exactly this: full-text search across Claude Code, OpenCode, and Hermes sessions in a terminal TUI. Happy to make it interop better with whatever context-restore approach lands here. (I'm the maker — github.com/coolcorexix/claude-grep)

---

### 2. [search] GitHub Issue #45076 — Cowork session history silently wiped on macOS
**Link:** https://github.com/anthropics/claude-code/issues/45076  
**Platform:** GitHub · anthropics/claude-code  
**Date:** April 8, 2026 (closed as duplicate)  
**Agent(s):** Claude Code (Cowork / Desktop, macOS)  
**Opportunity:** Low for direct engagement (closed); high demand signal. Pattern of silent data destruction appearing across Windows, Linux, and macOS.

**Context:** User opened Claude Desktop one morning to find *all* Cowork session chat histories and project metadata gone — multiple weeks of sessions. No app update, no user action. Support response: "current Cowork limitation." Mirrors Issue #33130 (Windows, ~1,967 sessions wiped) and a Linux auto-update reformat. The unifying theme is **silent destruction with no recovery path**.

**Drafted reply (if a linked active discussion is found):**
> If the sessions are still on disk as JSONL files under `~/.claude/projects/`, `ccfind` can search their content even if the Cowork UI has lost the index. Not a fix for the underlying bug, but it can help you find what you need while the data's still there. (Maker here — `brew install coolcorexix/tap/claude-grep`)

---

### 3. [search] DEV.to — "Searching 2500 Claude Code Sessions Was Painful So I Fixed It"
**Link:** https://dev.to/moshe_revah_b9ed24a93a59a/searching-2500-claude-code-sessions-was-painful-so-i-fixed-it-44fi  
**Platform:** DEV Community  
**Date:** Unknown (surfaced in June 2026 search, article appears recent)  
**Agent(s):** Claude Code  
**Opportunity:** Medium — comment section is open, a maker reply fits naturally.

**Context:** Developer accumulated 2,500 Claude Code sessions and found no way to search across them by content. `claude --resume` only scans titles. They built their own fix. This is the clearest first-person validation of the search pain in narrative form.

**Drafted comment:**
> Exact same itch — that's why I built `ccfind` (`brew install coolcorexix/tap/claude-grep`). It adds a tag per source ([c] for Claude Code, [o] for OpenCode, [h] for Hermes) so you can narrow by agent too. One extra trick: hit Ctrl-B on any search result to fork that session at that exact message — git-style prompt-tree branching, /btw-compatible. Would love to hear how your approach compares on 2500+ sessions. (I'm the maker)

---

## Pain — X / Twitter

### 1. [search] @coder_blvck — Manually grepping .claude/ as a "too stressful" workaround
**Link:** https://x.com/coder_blvck/status/2047986829954064549  
**Platform:** X / Twitter  
**Date:** ~April–May 2026 (estimated from status ID sequence)  
**Agent(s):** Claude Code  
**Opportunity:** HIGH — this is the most direct raw pain signal of the batch. Person is literally hand-crafting prompts to make Codex navigate the `~/.claude/` JSONL files for them.

**Context:** Tweet reads: *"Lmao, this is too stressful. Simply ask codex the following: You are continuing a session from Claude Code. Context: - The previous session is stored locally in the `.claude/` directory. - Locate the most recent session transcript (or the session titled '<session name>')"* — this is a person reinventing session search by delegating the grep to a second AI. The pain is raw and the workaround is hilariously brittle.

**Drafted reply (≤280 chars, no link — follow-up tweet if they engage):**
> Felt this. The whole "ask another AI to grep your AI's history" loop is peak 2026. Built a TUI for this — `ccfind` — terminal search across Claude Code, OpenCode, and Hermes sessions in <1s. No JSONL spelunking needed. (I'm the maker, happy to share if useful)

*Follow-up if they reply:* `brew install coolcorexix/tap/claude-grep` · github.com/coolcorexix/claude-grep

---

### 2. [search] @WolframRvnwlf — "TIL Claude Code silently deletes session history after 30 days"
**Link:** https://x.com/WolframRvnwlf/status/2032250687136932220  
**Platform:** X / Twitter  
**Date:** ~March 2026 (estimated); tweet continues to surface in shares  
**Agent(s):** Claude Code  
**Opportunity:** Medium — outside the 48h window but still being reshared; the thread has legs.

**Context:** *"PSA: TIL Claude Code silently deletes your session history after 30 days! The cleanupPeriodDays setting defaults to 30. Every time Claude Code starts, it purges all session transcripts older than that. No warning, no confirmation. If you rely on /resume or want to keep your [sessions]..."* Similar PSAs from @repligate and @RobertHaisfield confirm this has broad reach.

**Drafted reply (≤280 chars):**
> Even with cleanupPeriodDays set high, `--resume` only searches by title — you still can't find a session by what you talked about. Built `ccfind` to fill that gap: full-text search across all local sessions. (Maker here — will drop link if you want it)

---

### 3. [branching] @VadimKrutov — Tip: "branch a Claude Code session like a git branch"
**Link:** https://x.com/VadimKrutov/status/2052306267150946403  
**Platform:** X / Twitter  
**Date:** ~May 2026 (estimated from status ID)  
**Agent(s):** Claude Code  
**Opportunity:** Medium-High — the tip is friendly/educational, a reply fits naturally; the audience is exactly the branching-pain market.

**Context:** *"You can branch a Claude Code session like a git branch. `claude --continue --fork-session` copies the conversation and switches you into it. Original stays intact. Name sessions with -n so you can always find your way back."* — this is an active practitioner solving branching pain manually via CLI flags. Strong signal that the underlying desire (checkpoint + fork) is real and unsatisfied by native UX.

**Drafted reply (≤280 chars):**
> Nice tip! The gap I still feel: forking from a *past* session's specific message, not just the current one. Built Ctrl-B in ccfind for this — visual prompt-tree branching from any search result row, /btw-compatible with CC's native fork format. (Maker — happy to share)

---

### 4. [search] @NeoAIForecast — Hermes Agent sessions: "branch conversations" listed as a key upgrade
**Link:** https://x.com/NeoAIForecast/status/2044521045013762389  
**Platform:** X / Twitter  
**Date:** ~April–May 2026 (estimated)  
**Agent(s):** Hermes Agent (NousResearch)  
**Opportunity:** Medium — thread is educational, a maker reply with the cross-agent angle fits.

**Context:** *"This is the Hermes Agent article you need! ...One of the biggest upgrades is learning how to manage sessions properly: >resume by title >rename threads >branch conversations >export history"* — the framing of "branch conversations" as an *upgrade* for Hermes users confirms the demand exists on the Hermes side, not just Claude Code. This is the best Hermes-specific signal found this week.

**Drafted reply (≤280 chars):**
> The cross-agent angle is interesting — I built `ccfind` to search across Hermes + Claude Code + OpenCode sessions from one TUI. Hermes sessions tagged [h], so you can filter or compare. Ctrl-B branches from any past message. (Maker — link in reply if helpful)

---

## Show-your-work — Reddit / HN

### 1. Ask HN: What are you working on? (June 2026) — **LIVE RIGHT NOW**
**Link:** https://news.ycombinator.com/item?id=48528779  
**Platform:** Hacker News  
**Date:** June 2026 — posted ~8 hours ago at time of this scan (today's thread)  
**Opportunity:** **HIGH — top priority this week.** This is the monthly "what are you working on?" megathread. It was live at scan time with very fresh activity. HN readers are exactly the right audience: CLI tool users, AI-coding-workflow hackers, people building on top of Claude Code. This thread has warm norms for short genuine project intros.

**Drafted comment:**
> Building **ccfind** — a unified content search TUI for AI coding agent session history (`ccfind` command, runs in the terminal).
>
> The problem: `claude --resume` matches session titles only; if you want to find that authentication discussion from last week, you're grepping JSONL by hand. Same issue on OpenCode and Hermes Agent — each agent silos its history. `ccfind` searches all three at once, tags results [c]/[h]/[o] per source, and lets you jump straight into a resume.
>
> It also ships Ctrl-B prompt-tree branching: hit Ctrl-B on any search result row to fork that session at that exact past user message — a git-style checkpoint for AI conversations, /btw-compatible with Claude Code's native fork format.
>
> `brew install coolcorexix/tap/claude-grep` or `nix run github:coolcorexix/claude-grep` · https://github.com/coolcorexix/claude-grep

---

### 2. HN Show HN #48012399 — "Claude-Find: Pull Deep Memory from Across Your Claude Code Sessions"
**Link:** https://news.ycombinator.com/item?id=48012399  
**Platform:** Hacker News  
**Date:** ~May 2026 (based on item ID sequence vs. June thread at #48528779)  
**Opportunity:** Medium — a parallel tool in the same space; HN thread is likely still open for comments. Engaging here is a chance to contrast approaches (semantic MCP vs. terminal TUI + multi-agent support + branching) and pick up interested users.

**Drafted comment (value-first, not dismissive):**
> Great to see more tooling in this space. One angle I took differently in `ccfind`: it covers OpenCode and Hermes Agent sessions too, not just Claude Code — useful if you switch agents. Also terminal-native (no MCP setup) and ships Ctrl-B branching to fork sessions from past messages. Not better or worse, just different tradeoffs. (I'm the maker — github.com/coolcorexix/claude-grep)

---

## Show-your-work — X / Twitter

### 1. Build in Public community on X — ongoing weekly thread
**Link:** https://x.com/i/communities/1493446837214187523  
**Platform:** X / Twitter (Community, 260K+ members)  
**Date:** Ongoing — no specific thread from the last 48h surfaced, but weekly threads are standard in this community.  
**Opportunity:** Medium — the community actively welcomes short project updates. A plain "what I shipped this week" post with a screenshot of the TUI + the Ctrl-B feature would fit the norm. No specific thread URL to target right now; check the community feed manually for the current weekly prompt.

**Drafted post (≤280 chars, no link in first post):**
> Shipped this week: Ctrl-B prompt-tree branching in ccfind — fork any Claude Code / OpenCode / Hermes session at any past message, git-style checkpoint for AI convos. /btw-compatible. Built because --resume only searches titles, not content. (link in 🔁)

*Follow-up tweet:* `brew install coolcorexix/tap/claude-grep` · github.com/coolcorexix/claude-grep

---

### 2. @buildinpublic — standard build-in-public audience
**Link:** https://x.com/buildinpublic  
**Platform:** X / Twitter  
**Date:** Ongoing  
**Opportunity:** Low-Medium — broad audience, less targeted than the HN thread or a pain-reply. Best used if a specific thread or prompt emerges from this account. No actionable post found in last 48h.

---

## Market Signal (not a pain post, but worth noting)

**@lawrencecchen** shipped **cmux Vault** (status/2055248017025114609, ~May 2026) and **cmux history** (status/2061767722619572569, ~June 2026) — both add session search and history browsing across Claude Code, Codex, OpenCode, and Pi to the cmux terminal. The cmux history tweet has the highest observed status ID this week, suggesting it may be within the last 7 days. This is a direct competitor validating the multi-agent session-search demand. Worth tracking; not a thread to reply into (it's their product launch).

---

## Summary

**Today's split: ~60% [search] / ~40% [branching].**

The search-pain signal remains louder and more concrete — the GitHub issues and DEV.to article show users hitting the JSONL wall hard. Branching demand is more diffuse this week (mostly tips-sharing rather than raw frustration), likely because Claude Code 2.0's `/rewind` and `Esc+Esc` features absorbed some intra-session branching pain; the remaining unmet need is **cross-session, cross-agent** forking, which is exactly what Ctrl-B covers. **Top action today: post to Ask HN What are you working on? (June 2026) — it's live right now.**
