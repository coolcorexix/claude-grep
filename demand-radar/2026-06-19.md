# Demand Radar — 2026-06-19

---

## Pain — Reddit / HN / GitHub

### 1. [branching] "[FEATURE] Persist btw skill conversations and make them accessible in session history" — GitHub Issue #61105 · anthropics/claude-code — May 21, 2026
**Link:** https://github.com/anthropics/claude-code/issues/61105  
**Platform:** GitHub Issues (anthropics/claude-code)  
**Approx. date:** May 21, 2026  
**Context:** Someone filed a feature request asking Anthropic to persist `/btw` skill side-conversations into the main session history so they're accessible later. Today, `/btw` threads are ephemeral — they don't survive the session or appear in `/resume`.  
**Opportunity:** HIGH — this is the single sharpest overlap with claude-grep. The `/btw`-compatible branching in Ctrl-B is exactly what this issue is asking for. Maker can engage directly, transparently.

**Drafted reply:**
> Hi — I've been building something that tackles this. **claude-grep** (`ccfind`) ships a Ctrl-B prompt-tree feature that forks a Claude Code session at any past user message, and the fork is stored in the native `/btw`-compatible format — so it shows up in regular session history and is resumable. Full disclosure: I'm the maker and it's still early, but this is the exact use case it's designed for. Repo: https://github.com/coolcorexix/claude-grep — happy to hear what the ideal UX would look like for this issue.

---

### 2. [search] "Global session history view across all projects" — GitHub Issue #49095 · anthropics/claude-code — ~April 2026
**Link:** https://github.com/anthropics/claude-code/issues/49095  
**Platform:** GitHub Issues (anthropics/claude-code)  
**Approx. date:** ~April 2026  
**Context:** The built-in `/resume` picker is scoped to the current workspace folder — sessions from other projects are invisible in the UI even though the files exist on disk. Reporter calls it "a major workflow blocker for developers who work across multiple repos."  
**Opportunity:** HIGH — claude-grep's cross-project (and cross-agent) search is the direct answer. Engagement here is authentic and on-topic.

**Drafted reply:**
> This bit me constantly. Built **claude-grep** (`ccfind`) partly to solve it: it scans all `~/.claude/projects/` subdirectories (and OpenCode / Hermes Agent histories) and lets you search by content across every session regardless of which project you're currently in. Full disclosure, I'm the maker — `brew install coolcorexix/tap/claude-grep` if you want to try it. Would love feedback on whether this covers your workflow.

---

### 3. [search] Show HN: Search-sessions — Search all your Claude Code session history in <300ms — Hacker News #47128630 — ~Feb 24, 2026
**Link:** https://news.ycombinator.com/item?id=47128630  
**Platform:** Hacker News  
**Approx. date:** February 24, 2026  
**Context:** A community member built a standalone Rust binary to search Claude Code session history because the native UI was too limited. The thread has comments describing the underlying pain: sessions pile up, titles are useless (auto-named), and there's no way to find a session by what was discussed. Multiple similar "Show HN" tools appeared in Jan–Feb 2026 (ccrider, cc-sessions, claude-search), all solving the same problem independently — strong signal of unmet demand.  
**Opportunity:** MEDIUM — thread is ~4 months old and winding down, but it's a signal that the community is actively building in this space. The differentiator for claude-grep is multi-agent scope (OpenCode + Hermes) + branching, which none of these single-agent tools address. Post engagement is low-value here; better to reference it as validation when pitching elsewhere.

**Drafted reply (low-priority — only if the thread is still active):**
> Great idea — I ran into the same wall and ended up extending the concept in **claude-grep** (`ccfind`): same content-search across sessions, but it also covers OpenCode and Hermes Agent histories in one TUI, and adds Ctrl-B prompt-tree branching so you can fork any session from a past message. Full disclosure: I'm the maker. https://github.com/coolcorexix/claude-grep

---

## Pain — X / Twitter

### 1. [search] @0xSero — "Everyone should save their session history… all your Claude code sessions get deleted every month" — ~Jun 10–15, 2026
**Link:** https://x.com/0xSero/status/2050544304360165585  
**Platform:** X / Twitter  
**Approx. date:** ~Jun 10–15, 2026 (high snowflake ID, consistent with other June 2026 posts)  
**Context:** @0xSero posted a public service announcement warning Claude Code users that sessions are silently purged every 30 days, and pointing to their own GitHub repos to help save history. The post framed session data as valuable "training data" you should own. Thread is recent and received engagement from the developer community.  
**Opportunity:** HIGH — thread is recent, the pain is about preserving AND accessing session history. claude-grep solves the second step (search what you've saved). Reply can add clear value without being promotional.

**Drafted reply (≤280 chars, no outbound link in first tweet):**
> Smart move. One step further: once you've saved those sessions, you can actually *search inside them* by content — not just scroll by title. `ccfind` does that across Claude Code, OpenCode + Hermes from one TUI. Disclosure: I built it.

**Follow-up tweet (link goes here):**
> → github.com/coolcorexix/claude-grep

---

### 2. [branching] @MindBranches — Session management article: "Wrong path, same task → /rewind (esc esc)" — ~Jun 9–12, 2026
**Link:** https://x.com/MindBranches/status/2044733281204387983  
**Platform:** X / Twitter  
**Approx. date:** ~Jun 9–12, 2026 (ID ~2044xxx)  
**Context:** @MindBranches posted a concise summary of Claude Code session management tactics: continue / /clear / /rewind / /compact / summarize-a-failed-branch. The tweet signals that developers are actively learning when to use /rewind for wrong turns. The key gap: /rewind is destructive (it discards the wrong-path branch entirely). claude-grep's Ctrl-B preserves both branches.  
**Opportunity:** MEDIUM — the tweet is informational rather than expressing frustration, so a reply adding the "you can also keep the branch" angle reads as genuinely additive.

**Drafted reply (≤280 chars):**
> Good summary. One gap: /rewind discards the alternate path. `ccfind` Ctrl-B forks at any past message instead — both branches stay in history, resumable. Uses /btw-compatible format so it plays nice with native Claude Code. Disclosure: I built it.

---

### 3. [search] @WolframRvnwlf — "PSA: TIL Claude Code silently deletes your session history after 30 days!" — Mar 13, 2026
**Link:** https://x.com/WolframRvnwlf/status/2032250687136932220  
**Platform:** X / Twitter  
**Approx. date:** March 13, 2026  
**Context:** A well-known developer in the Claude Code community (@WolframRvnwlf) posted a PSA about the 30-day auto-deletion of session transcripts, explaining how to set `cleanupPeriodDays` to preserve history. The post got traction and follow-on posts from others (j⧉nus, Rob Haisfield, Ahmad). Still appearing in search results 3+ months later, suggesting it's being surfaced by new users discovering the setting.  
**Opportunity:** LOW — thread is now 3+ months old and the "fix" (set cleanupPeriodDays) is well-known. Not worth a cold reply now. Monitor if @WolframRvnwlf posts follow-ups about *finding* sessions after saving them (that's the next pain).

**Drafted reply (hold — only post if there's a fresh follow-up from this account):**
> Now that sessions are saved, finding them by content is the next wall — `claude --resume` only shows titles. `ccfind` greps inside the JSONL transcripts across all projects + OpenCode/Hermes. Disclosure: I'm the maker. [link in reply]

---

## Show-your-work — Reddit / HN

### 1. Ask HN: What are you working on? (June 2026) — Hacker News #48528779 — Jun 18, 2026 (LIVE)
**Link:** https://news.ycombinator.com/item?id=48528779  
**Platform:** Hacker News  
**Approx. date:** June 18, 2026 — posted ~1 day ago, thread still active  
**Opportunity:** HIGH — this is the monthly "show your work" thread, explicitly designed for short project intros. HN culture rewards concise, honest, technically specific descriptions. A well-crafted comment here gets picked up by developers who live in this exact problem space.

**Drafted HN comment:**
> Working on **claude-grep** (`ccfind`) — a unified content-search TUI across all your Claude Code, OpenCode (sst), and Hermes Agent session histories.
>
> The problem: `claude --resume` matches session titles, not content. If you want to find "that session where I debugged the Stripe webhook handler," you're grepping JSONL files manually. Same story for OpenCode and Hermes — history is fragmented per project, per agent, per profile.
>
> `ccfind` indexes all of them, tags results by source ([c]/[h]/[o]), and lets you resume any session with a single keypress.
>
> It also ships Ctrl-B prompt-tree branching: fork any session at any past user message — like `git checkout -b` for an AI conversation. Stored in the native `/btw`-compatible format, so forks show up in regular Claude Code session history.
>
> Install: `brew install coolcorexix/tap/claude-grep` or `nix run github:coolcorexix/claude-grep`  
> Repo: https://github.com/coolcorexix/claude-grep

---

## Show-your-work — X / Twitter

Nothing actionable found in the last 48h. No clear open "build-in-public" or "show your work" invitation threads from X that were fresh as of June 18–19, 2026. Recommend checking @KP's "build in public" threads (x.com/thisiskp_) directly — KP posts weekly round-ups that sometimes invite shares, and the audience overlaps well with the Claude Code developer community. No specific URL to confirm a current open thread today.

---

## Summary

**Today's split: ~60% [search] / ~40% [branching]**

The search-pain signal continues to dominate in volume — GitHub issues #49095 and #41021 on anthropics/claude-code are sustained feature requests that Anthropic hasn't closed, and the community keeps building workaround tools (Search-sessions, ccrider, ccresume, claude-search). The loudest **new** signal is the @0xSero post from ~June 10–15 warning about 30-day deletion, which is landing with developers who haven't yet discovered `cleanupPeriodDays` and sets up the next natural pain: now that you've saved history, how do you find it?

The branching signal is sharper than usual today: GitHub Issue #61105 ("Persist btw skill conversations") is a near-exact statement of what claude-grep's Ctrl-B does, and it's only 4 weeks old — the best reply opportunity in this digest.

**Top 2 actions today:**
1. Reply to GitHub Issue #61105 (branching, /btw, directly on-topic, maker welcome)
2. Drop a comment in "Ask HN: What are you working on? (June 2026)" — thread is live right now
