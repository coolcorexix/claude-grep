# Demand Radar — 2026-06-29

> Sources searched: GitHub (MCP — `anthropics/claude-code`, `created:>2026-06-27`), X/Twitter (via Google `site:x.com`, ~12 queries), Reddit (via Google — returned no indexable fresh threads again today; consistent pattern across all June radars), Hacker News (Google, direct fetch blocked 403). GitHub issue dates are MCP-confirmed exact. X tweet dates are estimated from Snowflake ID ordering; none can be pinned to the hour without visiting each page. Window: last 48h target, hard cap 1 week.

---

## Pain — Reddit / HN / GitHub

Reddit returned nothing indexable again today. HN direct fetch blocked. GitHub is the live signal: three issues filed June 28 UTC cover [search] and [both] pain — and the FRESHEST is less than 12 hours old.

---

### 1. [search] GitHub #72167 — June 28, 2026 23:52 UTC (< 12 hours ago) ⭐ FRESHEST / not in prior radars
**"[BUG] VSCode extension sessions not appearing in sidebar after closing"**
[https://github.com/anthropics/claude-code/issues/72167](https://github.com/anthropics/claude-code/issues/72167) · anthropics/claude-code · open, 0 comments

**Context (Claude Code / VS Code / macOS):** Reporter @vinnodevega upgraded to v2.1.195 and discovered that all new sessions vanish from the sidebar the moment they close. The data is 100% on disk — `.jsonl` files with full `ai-title` entries exist at `~/.claude-vscode/projects/<project>/` — but the sidebar reads only the *old* `~/.claude/projects/` path and never picks up the new storage location. The extension migrated session storage without updating the session-list reader. It is a new instance of the exact "data intact, UI blind" pattern: the session exists, but native tooling can't surface it. Zero comments means the maker's presence is first-mover.

**Opportunity:** HIGH — filed less than 12 hours ago, zero comments, clear reproduction, emotionally sharp pain (you literally just had the conversation, you close the panel, it's gone). `ccfind` reads both `~/.claude/` and `~/.claude-vscode/` paths simultaneously — the storage-path divergence that breaks the sidebar is invisible to a reader that scans both directories.

**Drafted reply (GitHub comment):**
> The path divergence (`~/.claude/` vs `~/.claude-vscode/`) is exactly what makes native `--resume` pickers miss sessions too — different clients write to different directories and each picker reads only its own. While the sidebar reader gets updated to follow the new path, `ccfind` ([claude-grep](https://github.com/coolcorexix/claude-grep)) scans both storage locations simultaneously, so those "missing" sessions are immediately searchable and resumable by content. `brew install coolcorexix/tap/claude-grep` or `nix run github:coolcorexix/claude-grep`. (Disclosure: I'm the author.)

---

### 2. [search] GitHub #72012 — June 28, 2026 07:23 UTC (yesterday, 3 comments) / not in prior radars
**"[BUG] Agent View: reopening a stopped/completed background session respawns with a new session-id, prior conversation is lost (v2.1.195, macOS)"**
[https://github.com/anthropics/claude-code/issues/72012](https://github.com/anthropics/claude-code/issues/72012) · anthropics/claude-code · open, 3 comments, updated June 29 01:35

**Context (Claude Code / macOS / Agent View):** Reporter @ozwxy runs background agents, stops or completes them, then tries to reopen via Agent View. Each reopen spawns a fresh 8-line stub under a *new* session-id — "zero `user`/`assistant` message entries" — while the original transcript (~900 KB, dozens of messages) sits untouched at `~/.claude/projects/<project>/<original-session-id>.jsonl`. The `jobs/state.json` records only `linkScanPath` pointing at the session's *own* transcript; there is no `parentId`/`respawnOf` linking the stub back to the original. The workaround in the reporter's own bug description: *"`claude --resume <original-session-id>` restores the full conversation. Users must manually locate the original id from `~/.claude/projects/<project>/*.jsonl`."* That workaround is exactly the manual grep step `ccfind` replaces. 3 comments and last-updated Jan 29 01:35 means the thread is live.

**Opportunity:** HIGH — thread is active (updated < 3 hours ago), workaround explicitly names the grep-the-JSONL-directory workflow, 900 KB transcript at stake. The link between the bug and `ccfind` is precise: the tool finds sessions by content across all `.jsonl` files regardless of which session-id the UI registered.

**Drafted reply (GitHub comment):**
> The data is safe — the 900 KB transcript is intact, it just has the wrong session-id attached to the Agent View entry. The workaround in your own description (hunt for the original id in `~/.claude/projects/*/*.jsonl`) is the manual step that `ccfind` ([claude-grep](https://github.com/coolcorexix/claude-grep)) skips: it indexes all local session files and lets you search by *content* — a few keywords from the conversation — rather than hunting UUIDs. Found sessions resume with a single keypress. `brew install coolcorexix/tap/claude-grep`. (Disclosure: I'm the author.)

---

### 3. [both] GitHub #72121 — June 28, 2026 19:24 UTC (yesterday, 4 comments, updated June 29 00:31) / not in prior radars
**"[FEATURE] Save a finished chat as a skill or agent in one click"**
[https://github.com/anthropics/claude-code/issues/72121](https://github.com/anthropics/claude-code/issues/72121) · anthropics/claude-code · open, 4 comments

**Context (Claude Code):** Reporter @sndsh404 gives the clearest single description of combined [search] + [branching] pain seen this week: *"When I work on something complex with Claude, the first try is always generic. It gets good only after I correct it a bunch of times... By the end it's exactly right. But all of that lives in one chat. And I can't just keep working in that one chat. The longer it gets, the worse it runs... So I open a new chat, and now it is gone. I have to do the same iterations and corrections over again."* The feature request (auto-extract a skill from a finished chat) is one possible solution — but the root pain is that good session state can't be carried forward. The Ctrl-B branch-from-any-message pattern addresses this directly: instead of extracting rules manually or starting from scratch, fork from the message where the session "clicked."

**Opportunity:** MED-HIGH — 4 comments means this is getting traction, and the pain description is quotable. The maker's reply isn't positioning a competitor to the requested feature; it's showing a complementary workflow (branching) that addresses the same root pain. Mention Ctrl-B prominently.

**Drafted reply (GitHub comment):**
> The root problem you're describing — "good session context trapped in one long chat, can't carry it forward" — is what the `/btw`-compatible Ctrl-B prompt-tree feature in [claude-grep](https://github.com/coolcorexix/claude-grep) (`ccfind`) was built for. When you hit the message where the session "clicked" — where the formatting, the voice, the depth all landed right — you can press Ctrl-B on that row and fork a new Claude Code session starting from exactly that point. The fork carries the full history up to that message, so your next document/design/spec starts from the tuned state, not from scratch, without the context rot of continuing the same session. The branch shows up in `claude --resume` normally. `brew install coolcorexix/tap/claude-grep`. (Disclosure: my project.)

---

**Quick signal: [branching] #72113 — Jun 28, 18:12 UTC (yesterday, 1 comment)**
"[Feature Request] Allow resuming from rewound state without losing progress" — user accidentally rewinds and loses the progress they'd made since the rewind point; wants a `redo`-style path forward. No drafted reply needed today (thin thread, low comment count), but it confirms the /rewind destructiveness concern is live in the tracker.

---

## Pain — X / Twitter

X pages block direct fetch; dates below are Snowflake-ID-estimated (higher ID ≈ later; relative ordering reliable, exact date ±2 weeks).

---

### 1. [search] @0xSero — est. May 2026
**"Everyone should save their session history, did u know all your Claude code sessions get deleted every month unless you change that in settings.json? All that is valuable training data they're keeping! So you should too. I have a few repos on GH to help you do this. Share it"**
[https://x.com/0xSero/status/2050544304360165585](https://x.com/0xSero/status/2050544304360165585)

**Context (Claude Code — cross-platform):** The 30-day default retention cap is surfacing in X discourse repeatedly. @0xSero frames it as data ownership ("valuable training data they're keeping") and is actively pointing followers to GitHub repos for saving sessions. This is [search] pain in the "my history is silently disappearing" form rather than the "I can't find it" form — but both forms leave users unable to retrieve past sessions. The maker can position `ccfind` as the more usable tool since it doesn't require running backup scripts; it reads what's already there.

**Opportunity:** MED — check for a live reply thread before engaging; this post may be a few weeks old. If replies are still coming in, a tool mention fits naturally.

**Drafted reply (X, ≤280 chars, no outbound link in first reply):**
> The 30-day default is the hidden trap — and even within those 30 days, you still can't search *content*. I built `ccfind`: one TUI that searches across all Claude Code sessions (+ OpenCode + Hermes), no backup scripts needed. Local, no cloud. (Disclosure: my project — reply for link)

---

### 2. [search] @kylegalbraith (Depot) — est. Jan–Feb 2026
**"You can now start and resume Claude Code sessions directly from the Depot dashboard. Before: Hunt through terminal history to find your session ID, remember the CLI commands, hope you're resuming the right one. Now: Just start chatting with the same session from within Depot."**
[https://x.com/kylegalbraith/status/1988233258274844777](https://x.com/kylegalbraith/status/1988233258274844777)

**Context (Claude Code — tool launch):** Depot (CI/CD platform) shipped a Claude Code session-resume dashboard feature and marketed it explicitly using the pain language: *"Hunt through terminal history to find your session ID, remember the CLI commands, hope you're resuming the right one."* This is a [search] demand signal as a product launch — the pain is clear enough that a cloud platform built a UI around it. `ccfind` is the local/open-source answer for users who don't want to pipe their sessions through a cloud dashboard.

**Opportunity:** LOW for direct reply (Depot is a product, not a pain thread). HIGH as a market-signal: if Depot is shipping this as a feature, the session-resume search pain is well-established enough to be a product differentiator. Use in pitches.

*No drafted reply — this is competitive intelligence, not an engagement target.*

---

### 3. [branching] No new X branching pain threads confirmed in the last 48h.
Standing signals (@cadenbuild, @enesakar, @nicopreme Pi-hook) remain from prior radars and are outside the 1-week window. Monitor for new posts following the active GitHub branching issues (#72113, #72125).

---

## Show-your-work — Reddit / HN

### 1. "Ask HN: What are you working on? (June 2026)" — ⚠️ WINDOW LIKELY CLOSED
[https://news.ycombinator.com/item?id=48528779](https://news.ycombinator.com/item?id=48528779) · Hacker News · posted ~June 1, 2026

**Opportunity:** LOW today — June 28 radar flagged this as the last realistic day of active engagement. New top-level comments on a ~28-day-old WAYWO thread rarely surface. If the maker has not commented yet, skip this and wait for the July 2026 thread (typically posted first week of July).

---

### 2. "Ask HN: What is your (AI) dev tech stack / workflow?" — still open
[https://news.ycombinator.com/item?id=48413629](https://news.ycombinator.com/item?id=48413629) · Hacker News · posted ~June 20, 2026

**Context:** Thread specifically about AI developer workflow; session-management fragmentation across agents is a live pain in that audience. ~9 days old — late but not dead for HN workflow threads, which often get meaningful replies 2 weeks in. The GitHub signals from this week (storage-path divergence, background-session-id mismatch) give concrete proof-points to fold in.

**Opportunity:** MED — lower time pressure than WAYWO, higher audience relevance (every reply is from someone with an AI coding workflow).

**Drafted comment (HN style):**
> For session management across Claude Code, OpenCode, and Hermes Agent: `ccfind` (`brew install coolcorexix/tap/claude-grep` · `nix run github:coolcorexix/claude-grep`).
>
> The problem it solves: each agent writes sessions to a different local directory in a different format, and native `--resume` pickers match by session-title only — not content — and only within the current project directory. If you can't remember which session or which project, you're grepping JSONLs manually (and discovering, as #72167 filed yesterday shows, that even the grep directory changes between extension versions).
>
> ccfind indexes all three agents' session files, does full-text content search across all projects and all agents in one TUI, tags results [c]/[h]/[o] by source, and resumes any matching session with a single keypress. Also ships Ctrl-B prompt-tree branching (fork any session at any past user message — `/btw`-compatible, branch shows up in `claude --resume` normally). Site: https://claude-grep.vercel.app

---

## Show-your-work — X / Twitter

No new "what are you building?" or "show your work" invitation threads found in the last 48h on X. The `#BuildInPublic` feed is the standing venue; the highest-shareable demo asset remains a short screen recording of Ctrl-B branching showing the original session and the fork both appearing in `claude --resume` afterward — that visual has not been posted to X yet.

---

## Summary

**Today's split:** 3 [search] signals (GitHub #72167, #72012; X @0xSero), 1 [both] (GitHub #72121), 1 [branching] quick signal (#72113) — all from GitHub, all filed June 28, with #72167 the freshest at < 12h old. The recurring pattern this week: storage-path divergence and session-id mismatch are producing a new class of "data intact, UI blind" bugs that ccfind's direct-JSONL-read design sidesteps entirely. The [branching] category is thin on X today but #72121's vivid description of the "long session goes stale, new session loses context" loop is the sharpest combined-pain articulation seen this week — a natural Ctrl-B pitch.

**Recommended actions today:** (1) Reply to GitHub #72167 — < 12h old, zero comments, first-mover window. (2) Reply to GitHub #72012 — 3 comments, thread active (updated 3h ago at publish time). (3) Reply to GitHub #72121 — 4 comments, Ctrl-B is the exact complement to what they asked for. (4) Comment in HN AI dev stack thread (#48413629) — still open, highly relevant audience, concrete proof-points from this week's GitHub issues strengthen the pitch.
