# Demand Radar — 2026-06-14

> **Method note:** X/Twitter searched via Google `site:x.com`; tweet ages estimated from Snowflake ID analysis (confirmed anchors: ID 2022M ≈ Jan 2026, 2032M ≈ Mar, 2038M ≈ Apr, 2050M ≈ May, 2059M ≈ Jun 9). GitHub issues dated from metadata returned by web search. HN and Reddit return HTTP 403 to direct fetch; indexed via Google. Items already covered in the June 10–13 radars are excluded.

---

## Pain — Reddit / HN / GitHub

### 1. [search] "Session history lost after extension update (2.1.156 → 2.1.158)"
**[anthropics/claude-code #64082](https://github.com/anthropics/claude-code/issues/64082) · GitHub · late May 2026**

An update to the Claude Code VS Code extension (2.1.156 → 2.1.158) silently wiped the session history index; users can no longer see or resume sessions predating the update from the extension's picker. The raw JSONL files are still on disk, but the UI lost track of them. Multiple reporters across different OSes, suggesting a migration bug in the new version.

**Context:** Claude Code VS Code extension. Distinct from #63839 (CLI update on May 28 covered June 12) — this is the extension-side counterpart filed slightly later.

**Opportunity: HIGH** — active open bug, several users in the thread asking for a workaround. Content search via raw JSONL is the workaround; the maker can deliver it.

**Draft GitHub comment:**
> The sessions are almost certainly still on disk even though the picker can't see them. The extension reads from `~/Library/Application Support/Claude/claude-code-sessions/` (Mac) or `~/.config/Claude/claude-code-sessions/` (Linux) — each session is a JSONL file named by UUID.
>
> While the extension-side fix ships: I'm the maker of [ccfind / claude-grep](https://github.com/coolcorexix/claude-grep), which reads those JSONL files directly and does full-content search in a terminal TUI — completely independent of the extension picker, so this regression doesn't affect it. Hit Enter to resume any session you find. Transparent that it's my own tool; might be a useful bridge until the index repairs itself.

---

### 2. [search] "~20 sessions lost, only 11 survived - 2 months of work I paid for - gone"
**[anthropics/claude-code #61952](https://github.com/anthropics/claude-code/issues/61952) · GitHub · May 24, 2026**

User reports a macOS data-loss event after an update: 20 sessions vanished from the resume picker; only the 11 most recent survived. The sessions represent two months of accumulated work on a professional project. Labeled `data-loss / bug / macOS`. Open and unresolved.

**Context:** Claude Code macOS. The high emotional intensity ("I paid for this") and the specific session-count detail suggest the user is trying to recover something specific — a content-search tool is exactly what they need to confirm whether the files are recoverable.

**Opportunity: HIGH** — raw distress, practical help exists, no solution offered in the thread yet.

**Draft GitHub comment:**
> Before concluding the sessions are gone: check `~/.claude/projects/` (or `~/Library/Application Support/Claude/claude-code-sessions/`). The picker losing track of sessions is a different failure mode from actual file deletion — in most reports of this pattern, the JSONL files are still there.
>
> To verify and recover: I'm the maker of [ccfind / claude-grep](https://github.com/coolcorexix/claude-grep). It reads those raw files directly, bypassing the picker entirely, and lets you search by content — so if a file exists you can find it by searching for text you remember writing. `brew install coolcorexix/tap/claude-grep`. Disclosing that it's my tool; no charge.

---

### 3. [search] "[FEATURE] Session picker search should also filter by project path"
**[anthropics/claude-code #47581](https://github.com/anthropics/claude-code/issues/47581) · GitHub · ~April 2026**

When using `claude -r` or `/resume`, the picker's search bar matches on session titles, git branch, tag, and PR number — but not on the project working directory. If you work across many repos, you can't filter "show me only sessions in ~/work/api/" without remembering the session title. Multiple 👍 reactions; well-articulated feature request.

**Context:** Claude Code CLI. This is the cleanest articulation of the title-only search gap: users explicitly want to filter by *where* they were working, not just *what they named it*.

**Opportunity: MED** — April post, slightly outside window, but comments are still open and ccfind directly solves this gap today. A comment is additive, not competitive.

**Draft GitHub comment:**
> ccfind (claude-grep) does this today — when you search, it shows each result tagged with its project directory path so you can see at a glance which repo a session came from. You can also Ctrl-F to filter results live by path fragment (e.g., "api/") alongside the content query. It reads the same `~/.claude/projects/` JSONL files. Worth knowing while this is tracked upstream. Disclosure: I'm the maker — [github.com/coolcorexix/claude-grep](https://github.com/coolcorexix/claude-grep).

---

## Pain — X / Twitter

### 1. [search] @dgrcode — "I'm now Codex team — Codex finds the conversation via @EntireHQ by phrase"
**[@dgrcode](https://x.com/dgrcode/status/2059311188215783445) · X · ~June 9, 2026 — FRESHEST SIGNAL THIS WEEK**

Full tweet:
> "Anthropic gave me a refund, which means I'm now Codex team. First impression: 'I lost access to Claude in the middle of a task. Can you see the conversation that ends with this phrase?' codex: finds the conversation via @EntireHQ, then proceeds to fix imports and tests. 'Yes'"

A developer explicitly switched ecosystems — and is staying switched — because Codex's tooling can locate a prior conversation by searching its *content* (ending phrase). The @EntireHQ integration is doing what `claude --resume` + title-only search cannot. This is the single clearest "I left Claude because of the content-search gap" signal to surface in this radar series.

**Context:** Claude Code → Codex migration, content search as the deciding factor.

**Opportunity: HIGH** — recent, public, direct pain expression. The maker can reply with a concrete answer to the same question in the Claude ecosystem. Do NOT link in first reply.

**Draft reply (≤280 chars, no outbound link):**
> The gap you hit is real — Claude Code's resume is title-only. The equivalent for Claude sessions exists: ccfind (claude-grep) does full-content search across ~/.claude/projects/ JSONL files, including phrasing from anywhere in the conversation. If Claude's reliability improves, you'd have content search parity. Maker here — DM for details.

*(Follow-up tweet if they engage):*
> `brew install coolcorexix/tap/claude-grep` or `nix run github:coolcorexix/claude-grep` — github.com/coolcorexix/claude-grep

---

### 2. [search] @gully_point — "Accidentally deleted a very important Claude chat… absolutely no way to recover it now"
**[@gully_point](https://x.com/gully_point/status/2036312306464993477) · X · ~April 29, 2026**

> "Accidentally deleted a very important Claude chat with loads of data in it…and looks like there's absolutely no way to recover it now"

Classic unrecoverable-deletion grief post. No one in the replies offered a practical path. Claude Code stores session transcripts as JSONL files locally — deletion from the UI does not always purge the file.

**Context:** Claude Code (or possibly Claude.ai — tweet is ambiguous). If it's the CLI/Desktop app, a file-level recovery exists.

**Opportunity: MED** — 6 weeks old, but evergreen (this post pattern repeats weekly). Worth a reply that might help even if late; the value is also in getting the maker's name attached to the "how to recover a Claude session" search trail.

**Draft reply (≤280 chars, no outbound link):**
> If this was Claude Code Desktop/CLI: check ~/.claude/projects/ — UI deletions don't always scrub the underlying JSONL. Also, next time: set "cleanupPeriodDays": 99999 in settings.json and install ccfind to search across sessions before they're gone. Maker of ccfind — sorry this happened.

---

### 3. [both] @svpino (Santiago) — "20 Claude Code tips: 1. Esc+Esc: Rewind your session to any previous checkpoint"
**[@svpino](https://x.com/svpino/status/2050563493708112190) · X · ~May 2026**

A "20 Claude Code tips" thread where tip #1 is Esc+Esc rewind. The thread is getting engagement in replies from people who didn't know this existed — which signals the feature is real but the UX is undiscoverable. Several replies asking follow-ups about what happens to the "abandoned" branch (it disappears, covered by /rewind-fork bug in issue #55347, June 11 radar).

**Context:** Claude Code. Branching/rewind demand confirmed by the tip's position as #1 and its reply volume.

**Opportunity: MED** — tips thread replies are a soft show-your-work opportunity for the Ctrl-B angle; engagement is still live.

**Draft reply (≤280 chars, no outbound link):**
> Great list — Esc+Esc is underused. One limitation: the "forked" session doesn't always show in claude --resume (known bug #55347). For branching from any *specific past message* that does reliably appear in resume: ccfind's Ctrl-B writes a proper /btw-compatible fork. Maker here.

*(Follow-up with link if they engage.)*

---

## Show-your-work — Reddit / HN

### 1. No "Ask HN: What are you working on? (June 2026)" found
The June 2026 monthly HN "What are you working on?" thread is not yet indexed in search results (the June "Who is hiring?" thread at item #48357725 appeared, but the companion "working on?" thread has not surfaced). The May 2026 thread (item #48085993) was the recommended posting target in the June 10–12 radars; if not yet posted there, do so now — HN monthly threads stay open 4–6 weeks.

When the June thread appears, it will have an item ID in the ~48500000–48600000 range; check `news.ycombinator.com/ask` daily this week.

**Pre-written HN comment (ready to drop when the June thread appears):**
> **ccfind (claude-grep)** — unified full-text search + prompt-tree branching across Claude Code, OpenCode (sst), and Hermes Agent session histories.
>
> The problem: `claude --resume` matches session titles only. Across three agents and many projects, "which session had that auth debugging we did last Tuesday?" means manually grepping three different directories in three different formats. Most people just restart.
>
> What ccfind does: reads `~/.claude/projects/` (JSONL), `~/.local/share/opencode/` (SQLite), and `~/.hermes/` (flat files), unifies them in one TUI, and does full-content search in <300ms. Results tagged `[c]`/`[h]`/`[o]` by agent. Hit Enter to resume; Ctrl-F to filter by path or keyword live.
>
> Feature I'm more excited about: **Ctrl-B on any past user-message row** forks the session at that exact point — writes a new JSONL in Claude Code's native fork format with a `parentSessionId` back-reference, so the branch shows in `claude --resume`. A git-style "branch from here" for AI conversations. `/btw`-compatible.
>
> `brew install coolcorexix/tap/claude-grep` · `nix run github:coolcorexix/claude-grep` · https://claude-grep.vercel.app

---

### 2. r/SideProject weekly thread — not found via search
Web-indexed Reddit searches failed to surface a current weekly "what did you make?" sticky from r/SideProject or r/coolgithubprojects. Check these subreddits directly at reddit.com/r/SideProject (sort by Hot — the weekly sticky is usually pinned) and r/ClaudeAI (look for a "weekly tools" or "show and tell" thread). If a thread is live, use the HN comment draft above trimmed for Reddit tone (slightly more casual, add a line on what pain it solves for Claude Code users specifically).

---

## Show-your-work — X / Twitter

No dedicated "what are you building" invitation thread found in the last 48 hours that meets the quality bar.

**Standing recommendation from June 11–12 radars:** the @ClaudeDevs `/fork`/`/branch` rename thread (status/2061947411141169494, June 2, 2026) is now 12 days old — engagement has likely cooled. Do not post there today.

**Best ambient opportunity this week:** watch the replies under @svpino's 20-tips thread (listed above) — it is currently generating follow-up questions about Esc+Esc behavior where a Ctrl-B reply lands naturally.

---

## Summary

**Today's split: 5 [search] : 1 [branching] : 1 [both].** Search pain is strongly dominant again. The most impactful find is @dgrcode's June 9 post — a developer explicitly citing content-search capability as the reason they migrated from Claude to Codex. That's the sharpest possible product-market fit signal and the top priority reply today. GitHub signals cluster around session-loss bugs tied to two successive updates (extension 2.1.156→2.1.158 and CLI May 28), both still open and unresolved. No fresh branching pain found on X or Reddit/HN this cycle; all branching demand is carried over from prior weeks' open issues (#32631, #55347). The June HN "What are you working on?" thread has not appeared yet — it is the week's pending highest-leverage show-your-work target.
