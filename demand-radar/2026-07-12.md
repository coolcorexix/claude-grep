# Demand Radar — 2026-07-12

---

## Pain — Reddit / HN / GitHub

### 1. [search] GitHub #70312 — Desktop sidebar hides sessions after machine migration (data intact, UI blindspot)
**Link:** https://github.com/anthropics/claude-code/issues/70312 · **Platform:** GitHub (anthropics/claude-code) · **Date:** June 23, 2026 (~19 days ago) · Closed as duplicate

**Context (Claude Code Desktop):** A user migrated their `.claude` folder byte-for-byte from one Windows PC to another, recreated the identical project path, and found Claude Code Desktop's sidebar shows zero historical sessions — despite 10 sessions / 4,426 messages / 3.1M tokens sitting intact on disk. The Summary panel correctly aggregates the stats (confirming the files are readable), but the session list won't surface them. Root cause per Anthropic: a provenance check that blocks sessions not created on the current machine's environment. Closed as duplicate of earlier issues (#18645, #25524, #26452), meaning the pattern has been recurring for months with no fix.

**Opportunity:** HIGH. ccfind reads the raw `.jsonl` files from `~/.claude/projects/` directly — it has no provenance check, no machine-ID gate, no UI indexing layer. A user with a migrated `.claude` folder who can't find their sessions in the Desktop UI can use `ccfind` to recover full content-search access immediately. This is a very concrete, specific recovery story to tell in one of the duplicate parent issues.

**Drafted reply (GitHub, parent issues #18645 or #25524):**
> The UI provenance check is the right fix long-term, but if your `.jsonl` files are intact on disk you can search and resume sessions right now without the Desktop sidebar. `ccfind` (via `brew install coolcorexix/tap/claude-grep`) reads `~/.claude/projects/` directly — no indexing, no machine-ID check — and drops you into `claude --resume` on whichever session matches your content search. Might unblock you while Anthropic works on the migration path. Disclosure: I built it.

---

### 2. [search] Competitor signal: Dicklesworthstone/cass — 954 stars, 22+ providers, first appearance in today's search
**Link:** https://github.com/Dicklesworthstone/coding_agent_session_search · **Platform:** GitHub · **Date:** Not confirmed (well-maintained, 4,384 commits, 954 stars, 118 forks)

**Context (Claude Code + OpenCode + Hermes + 19 more):** `cass` (Coding Agent Session Search) is a unified TUI+CLI that normalises local session history from 22+ providers — Claude Code, OpenCode, Hermes Agent, Codex, Gemini CLI, Cursor, Aider, Cline, Amp, ChatGPT, Kimi Code, Qwen Code, Factory (Droid), and more — into a searchable knowledge base. Sub-60ms lexical search via edge n-gram indexing, optional local semantic search (no network), three-pane TUI, multi-machine SSH/rsync sync, HTML export with AES-256 encryption, and a "robot mode" for AI agent consumption. This is the most feature-complete multi-agent session search tool seen this radar cycle — more comprehensive than raine/claude-history (Claude Code only) or cmux Vault (4 providers, GUI).

**Opportunity:** N/A for a reply. HIGH competitive intelligence. ccfind differentiators to emphasise when cass comes up: (1) zero-index — no database setup, pure ripgrep+fzf, works out of the box in seconds; (2) Ctrl-B prompt-tree branching from search results (cass has no branching); (3) native agent detection — auto-discovers which of the three agents are installed rather than requiring configuration. The cass user who wants to branch from a past message and resume with `claude --resume` is ccfind's clearest acquisition target.

---

### 3. [search] GitHub #58267 — `/sessions command — browse previous sessions with summaries and resume`
**Link:** https://github.com/anthropics/claude-code/issues/58267 · **Platform:** GitHub (anthropics/claude-code) · **Date:** May 12, 2026 · Closed as duplicate

**Context (Claude Code):** Feature request for a `/sessions` command that would list recent sessions with auto-generated summaries (derived from the first user message or session topic), let users view what was accomplished, and resume from the selection. The requester notes: "Session transcripts in `~/.claude/projects/` are not browsable — `/resume` shows bare session IDs and timestamps with no context." Closed as duplicate, confirming Anthropic is aware and folding this into a wider effort. That the issue keeps being filed as "new" (this is at least the 4th distinct variant) shows the pain is discovered fresh by each new wave of power users.

**Opportunity:** LOW for a direct comment (closed/duplicate). HIGH as recurring demand signal — the phrase "not browsable / no context in session IDs" is the exact user language that positions ccfind's content-search-by-what-was-discussed angle. Worth seeding this framing in README / landing page copy.

---

## Pain — X / Twitter

### 1. [branching] @ClaudeDevs — `/fork` redesigned as background agent, old `/fork` renamed `/branch`
**Link:** https://x.com/ClaudeDevs/status/2061947411141169494 · **Platform:** X / Twitter · **Date:** ~June 2026 (tweet ID 2061947411..., ~4–6 weeks ago)

**Context (Claude Code):** Official Anthropic announcement: `/fork` now spawns a background agent that inherits full context (system prompt, tools, history, model, prompt cache) and returns results to the main session. The old `/fork` behaviour (copy transcript to a new interactive session you drive) is renamed `/branch`. The replies to this thread show recurring confusion: users forked sessions in the old sense and can't find the resulting branches, or they want to fork at a specific past message rather than the current head. The rename compounds the discoverability problem — "my branches aren't showing in `/resume`" appears in several replies.

**Opportunity:** MED. The reply thread is the right place to mention that Ctrl-B in ccfind forks at any past user message (not just current head), producing a standard `.jsonl` fork file that's immediately resumable with `claude --resume` — no separate "find my branch" step. Keep it value-first; this is an engaged technical audience that will ask follow-up questions.

**Drafted X reply (≤280 chars, hold the link — put in follow-up or bio):**
> The `/branch` rename is cleaner but the "where's my fork?" problem persists when you want to branch from message 23 rather than the current head. I built a TUI called ccfind that lets you hit Ctrl-B on any past user message and fork there — the output is a standard claude session file, shows up in `claude --resume` immediately. Link in bio.

---

### 2. [search] @ryanvogel — "just tried out claude code after only using opencode for 4 months and dang guys, you live with this?"
**Link:** https://x.com/ryanvogel/status/2060299369820397623 · **Platform:** X / Twitter · **Date:** ~June 2026 (tweet ID 2060299..., ~4–6 weeks ago)

**Context (Claude Code + OpenCode):** Cross-agent user who's been living in OpenCode full-time tries Claude Code and finds the UX jarring enough to post publicly with a screenshot. The screenshot context isn't visible in search results, but the tone ("you live with this?") and the growing cross-agent migration pattern — power users who run both tools and accumulate history in two separate formats — is the exact fragmented-history problem ccfind solves. The replies likely contain further comparison pain.

**Opportunity:** MED. The specific friction isn't confirmed (could be anything in Claude Code's UX), so don't assume it's session history. Reply with a question or a specific cross-agent angle: "Was the session-history UX part of the adjustment? I run both and built a TUI that searches across both (and Hermes) from one place." This opens a genuine conversation without spamming a solution at an unconfirmed problem.

**Drafted X reply (≤280 chars):**
> Curious what hit hardest — was the session-resume UX part of it? Built a TUI (ccfind) that searches across Claude Code, OpenCode, and Hermes sessions from one place since switching between them tends to fragment your history. If that's not the pain point, ignore this!

---

### 3. No clean third X slot this 48h window
The branching-side PSA cluster and the @ClaudeDevs /fork thread are the two live X engagement opportunities today. The cleanupPeriodDays cluster (covered in depth in the 2026-07-11 digest) continues to generate echo posts but no new standalone signal with a first-reply slot. The @0xSero cross-agent migration post (status/2000841530..., Jan 2026) is too stale for outreach. Recommend monitoring for a new PSA discovery post that would open a fresh reply slot.

---

## Show-your-work — Reddit / HN

### 1. Ask HN: What are you working on? (July 2026) — URGENT: 7 days old, closing window
**Link:** https://news.ycombinator.com/item?id=48785376 · **Platform:** Hacker News · **Date:** ~July 5, 2026 (7 days old today — these threads typically stop receiving attention by day 10–14)

**Context:** Monthly AYWO thread. Yesterday's digest flagged this as the highest-leverage show-your-work venue and noted the ctx competitor (cross-Claude Code + Codex resume, Show HN ~July 4) seeded the session-management audience on HN this month. If not yet posted, today is the day — the thread will pass its attention peak in 3–7 days. The cass competitor (954 stars) is visible proof the "multi-agent session search" category is real and has audience; name it in the comment to set shared context.

**Drafted HN comment (post today if not yet done):**
> Building ccfind / claude-grep — a terminal TUI to search all your AI coding-agent session history by content, not just session title or ID. The problem: `claude --resume` only matches the first prompt or title; OpenCode stores sessions in SQLite and Hermes Agent in a different JSON schema — there's no native cross-agent search. ccfind auto-detects which agents are installed, searches full transcript body, tags results by source ([c]/[h]/[o]), and resumes any match with a single keypress.
>
> Also ships Ctrl-B prompt-tree branching: fork any Claude Code session at any past user message, writes a /btw-compatible fork file, immediately resumable with `claude --resume`. Useful when a long session drifted across topics and you want to rewind to an earlier branch point — closer to git checkout than to /rewind.
>
> raine/claude-history (386★, Claude Code only, Rust) and cass (954★, 22 providers, database-backed) are in the space too; ccfind's angle is zero-index (grep+fzf, no setup), cross-agent in 3 lines of install, branching from search results.
>
> Install: `brew install coolcorexix/tap/claude-grep` or `nix run github:coolcorexix/claude-grep` · Repo: github.com/coolcorexix/claude-grep

---

### 2. r/SideProject — no confirmed thread found
No July 2026 weekly/monthly self-promotion thread surfaced via search. Visit reddit.com/r/SideProject directly, filter by "New," and look for the current "I Made This" or "Monthly Launch" megathread. The drafted post from the 2026-07-11 digest (mentioning the multi-agent angle and Ctrl-B) is ready to drop.

---

## Show-your-work — X / Twitter

No dedicated "what are you building" invitation thread surfaced on X in the last 48h. The @ClaudeDevs /fork update thread (item 1 in Pain — X above) doubles as a show-your-work adjacency: a well-placed Ctrl-B description in the reply thread will surface ccfind to the audience most likely to care about conversation branching. That's the best X show-your-work slot this week.

---

## Summary

**Today's split: ~70% [search] / ~30% [branching].**

The lead signal today is GitHub #70312 (machine migration leaves sessions invisible in Desktop UI, June 23) — ccfind's file-direct approach is a concrete recovery story with no equivalent in the native tool. The cass competitor (954 stars, 22 providers) is the biggest competitive development seen this week; it validates the multi-agent session search market at scale but doesn't have branching, which remains ccfind's cleanest differentiator. The HN AYWO July 2026 thread (item 1, Show-your-work) is in its closing window — post today if not yet done. Branching signals remain quieter than search signals this week; the @ClaudeDevs /fork rename thread is the best place to surface Ctrl-B to a branching-aware audience.
